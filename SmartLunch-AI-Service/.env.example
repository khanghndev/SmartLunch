# SmartLunch AI Service
# Copy to .env and edit if needed

# SmartLunch Backend API base URL (used to fetch dishes + dish ingredients)
BACKEND_BASE_URL=http://localhost:5001

# Bearer token of an Admin user (must have permission:dishes.read etc.)
# BACKEND_ADMIN_BEARER_TOKEN=your_jwt_here

# CORS_ORIGINS=*

